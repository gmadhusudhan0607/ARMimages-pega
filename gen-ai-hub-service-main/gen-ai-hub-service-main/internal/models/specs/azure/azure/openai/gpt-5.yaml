infrastructure: azure
provider: azure
creator: openai
metadata:
  description: "OpenAI GPT-5 model family"
  documentation: "https://platform.openai.com/docs/models/gpt-5"
models:
  - name: gpt-5
    version: "2025-08-07"
    label: "GPT-5"
    id: openai.gpt-5-2025-08-07
    functionalCapabilities: ["chat_completion"]
    endpoints:
      - path: "/chat/completions"
    capabilities:
      features: ["streaming", "functionCalling", "parallelFunctionCalling", "toolCalling", "parallelToolCalling", "jsonMode", "structuredOutput"]
      inputModalities: ["text", "image"]
      outputModalities: ["text"]
      mimeTypes: ["image/png", "image/jpeg", "image/gif", "image/webp"]
    parameters:
      maxInputTokens:
        title: "Max Input Tokens"
        description: "Maximum number of tokens (words or subwords) for input"
        type: "integer"
        maximum: 400000
        required: false
      maxOutputTokens:
        title: "Max Output Tokens"
        description: "Maximum number of tokens to generate"
        type: "integer"
        default: null
        maximum: 128000
        required: false
      temperature:
        title: "Temperature"
        description: "Controls randomness of the generated output"
        type: "float"
        default: 1.0
        maximum: 2.0
        minimum: 0.0
        required: false
      topP:
        title: "Top P"
        description: "Nucleus sampling parameter controlling diversity of the output"
        type: "float"
        default: 1.0
        minimum: 0.0
        maximum: 1.0
        required: false
  - name: gpt-5-nano
    version: "2025-08-07"
    label: "GPT-5-Nano"
    id: openai.gpt-5-nano-2025-08-07
    functionalCapabilities: ["chat_completion"]
    endpoints:
      - path: "/chat/completions"
    capabilities:
      features: ["streaming", "functionCalling", "parallelFunctionCalling", "toolCalling", "parallelToolCalling", "jsonMode", "structuredOutput"]
      inputModalities: ["text", "image"]
      outputModalities: ["text"]
      mimeTypes: ["image/png", "image/jpeg", "image/gif", "image/webp"]
    parameters:
      maxInputTokens:
        title: "Max Input Tokens"
        description: "Maximum number of tokens (words or subwords) for input"
        type: "integer"
        maximum: 400000
        required: false
      maxOutputTokens:
        title: "Max Output Tokens"
        description: "Maximum number of tokens to generate"
        type: "integer"
        default: null
        maximum: 128000
        required: false
      temperature:
        title: "Temperature"
        description: "Controls randomness of the generated output"
        type: "float"
        default: 1.0
        maximum: 2.0
        minimum: 0.0
        required: false
      topP:
        title: "Top P"
        description: "Nucleus sampling parameter controlling diversity of the output"
        type: "float"
        default: 1.0
        minimum: 0.0
        maximum: 1.0
        required: false
  - name: gpt-5-mini
    version: "2025-08-07"
    label: "GPT-5-Mini"
    id: openai.gpt-5-mini-2025-08-07
    functionalCapabilities: ["chat_completion"]
    endpoints:
      - path: "/chat/completions"
    capabilities:
      features: ["streaming", "functionCalling", "parallelFunctionCalling", "toolCalling", "parallelToolCalling", "jsonMode", "structuredOutput"]
      inputModalities: ["text", "image"]
      outputModalities: ["text"]
      mimeTypes: ["image/png", "image/jpeg", "image/gif", "image/webp"]
    parameters:
      maxInputTokens:
        title: "Max Input Tokens"
        description: "Maximum number of tokens (words or subwords) for input"
        type: "integer"
        maximum: 400000
        required: false
      maxOutputTokens:
        title: "Max Output Tokens"
        description: "Maximum number of tokens to generate"
        type: "integer"
        default: null
        maximum: 128000
        required: false
      temperature:
        title: "Temperature"
        description: "Controls randomness of the generated output"
        type: "float"
        default: 1.0
        maximum: 2.0
        minimum: 0.0
        required: false
      topP:
        title: "Top P"
        description: "Nucleus sampling parameter controlling diversity of the output"
        type: "float"
        default: 1.0
        minimum: 0.0
        maximum: 1.0
        required: false
  - name: gpt-5-chat
    version: "2025-08-07"
    label: "GPT-5-Chat"
    id: openai.gpt-5-chat-2025-08-07
    functionalCapabilities: ["chat_completion"]
    endpoints:
      - path: "/chat/completions"
    capabilities:
      features: ["streaming"]
      inputModalities: ["text", "image"]
      outputModalities: ["text"]
      mimeTypes: ["image/png", "image/jpeg", "image/gif", "image/webp"]
    parameters:
      maxInputTokens:
        title: "Max Input Tokens"
        description: "Maximum number of tokens (words or subwords) for input"
        type: "integer"
        maximum: 128000
        required: false
      maxOutputTokens:
        title: "Max Output Tokens"
        description: "Maximum number of tokens to generate"
        type: "integer"
        default: null
        maximum: 16384
        required: false
      temperature:
        title: "Temperature"
        description: "Controls randomness of the generated output"
        type: "float"
        default: 1.0
        maximum: 2.0
        minimum: 0.0
        required: false
      topP:
        title: "Top P"
        description: "Nucleus sampling parameter controlling diversity of the output"
        type: "float"
        default: 1.0
        minimum: 0.0
        maximum: 1.0
        required: false
